{
	"name": "db",
	"version": "0.0.0",
	"description": "",
	"main": "pocketbase",
	"scripts": {
		"dev": "cross-env ./pocketbase serve",
		"migration:flatten": "rimraf --glob pb_migrations/* && cross-env ./pocketbase migrate collections && cross-env ./pocketbase migrate history-sync"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"type": "module",
	"devDependencies": {
		"cross-env": "^7.0.3",
		"rimraf": "^5.0.5"
	}
}
